' String benchmark: concatenation and builtin string calls.

Sub Main()
    Dim acc As String
    Dim i As Int32
    Dim n As Int32
    For i = 1 To 800000
        acc = "item-" & i
        n = n + Len(acc)
    Next i
    Console.WriteLine("n=" & n)
End Sub
