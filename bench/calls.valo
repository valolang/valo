' Recursive call benchmark: exercises frame setup, parameter binding,
' and function dispatch.

Function Fib(ByVal n As Int32) As Int32
    If n < 2 Then
        Return n
    End If
    Return Fib(n - 1) + Fib(n - 2)
End Function

Sub Main()
    Console.WriteLine("fib(27)=" & Fib(27))
End Sub
