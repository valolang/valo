
Class Box
    Private value As Long

    Private Sub Class_Initialize()
        value = 21 \ 2
    End Sub

    Public ReadOnly Default Property Item() As Integer
        Get
            Item = value
        End Get
    End Property
End Class

Function MakeBox() As Object
    MakeBox = New Box()
End Function

Sub Main()
    Dim box As New Box
    Console.WriteLine(box)

    Dim values() As Integer
    ReDim values(0 To 2)
    values(2) = 5
    Console.WriteLine(LBound(values, 1) & ":" & UBound(values, 1) & ":" & values(2))
    Erase values

    Dim maybe As Variant
    maybe = Null
    Console.WriteLine(IsNull(maybe))
    Console.WriteLine(TypeName(MakeBox()))
    Console.WriteLine(IIf(StrComp("a", "A", 1) = 0, CStr(Sgn(-3)), "no"))
End Sub
