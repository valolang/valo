
Sub Main()
    Dim fixed(3) As Integer
    Dim dynamic() As Integer
    Dim item As Integer
    Dim total As Integer

    fixed(1) = 10
    fixed(3) = 30
    For Each item In fixed
        total = total + item
    Next item

    ReDim dynamic(2)
    dynamic(0) = 7
    dynamic(1) = 9

    Console.WriteLine("fixed " & LBound(fixed) & ":" & UBound(fixed) & " total " & total)
    Console.WriteLine("dynamic " & LBound(dynamic) & ":" & UBound(dynamic) & " values " & dynamic(0) & "," & dynamic(1))
End Sub
